import { AffaireDirective } from './affaire.directive';

describe('AffaireDirective', () => {
  it('should create an instance', () => {
    const directive = new AffaireDirective();
    expect(directive).toBeTruthy();
  });
});
