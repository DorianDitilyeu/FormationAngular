import { JourDirective } from './jour.directive';

describe('JourDirective', () => {
  it('should create an instance', () => {
    const directive = new JourDirective();
    expect(directive).toBeTruthy();
  });
});
